# Demo fixtures

`sample_dove_vendor.csv` is synthetic test/demo data only. It is intentionally isolated from production paths.
One caption contains prompt-injection-like text to prove that vendor content remains untrusted evidence rather than application instructions.

## Priority snapshot template

`priority_snapshot_template.json` is a **shape template only**. Its placeholders are intentionally invalid and it cannot be applied as a real priority result. After a dataset is analysed, replace the dataset/Trend IDs and fields only with an actual authoritative existing prioritisation output. Choice A forbids Radar from deriving those values itself.
