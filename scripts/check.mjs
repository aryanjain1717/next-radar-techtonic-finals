import fs from 'node:fs';import path from 'node:path';import {spawnSync} from 'node:child_process';import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const required=['package.json','README.md','AI_STUDIO_MASTER_PROMPT.md','ARCHITECTURE_AND_COHERENCY.md','GOOGLE_AI_STUDIO_HANDOFF.md','FINAL_VALIDATION_REPORT.md','server/index.mjs','server/pipeline.mjs','server/ask-radar.mjs','client/src/App.jsx','demo/sample_dove_vendor.csv'];for(const f of required)if(!fs.existsSync(path.join(root,f)))throw new Error(`Missing required file: ${f}`);const files=[];const walk=d=>{for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(p.endsWith('.mjs'))files.push(p);}};walk(path.join(root,'server'));walk(path.join(root,'tests'));for(const f of files){const r=spawnSync(process.execPath,['--check',f],{encoding:'utf8'});if(r.status!==0)throw new Error(`Syntax check failed: ${f}\n${r.stderr}`);}console.log(`Syntax checked ${files.length} server/test modules.`);console.log('Required project files present.');
